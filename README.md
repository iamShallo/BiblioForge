# BiblioForge
An AI-driven pipeline that transforms raw book data into a curated catalog. It cleans titles, fetches reliable metadata via APIs, and uses an LLM to generate intelligent summaries and tags. A Streamlit dashboard ensures quality through human-in-the-loop validation, allowing users to easily approve data or restart the search.
